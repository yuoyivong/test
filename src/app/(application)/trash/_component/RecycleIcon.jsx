import React from 'react';
import {Refresh} from "iconsax-react";

const RecycleIcon = ({ onClick }) => {
    return (
        <Refresh size="20" color="#105DFF" onClick={onClick}/>
    );
};

export default RecycleIcon;
